import 'package:flutter/material.dart';

class WaterPixel extends StatelessWidget {
  const WaterPixel({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(0.1),
      child: Container(color: const Color(0xFF121252)),
    );
  }
}
