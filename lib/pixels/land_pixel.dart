import 'package:flutter/material.dart';

class LandPixel extends StatelessWidget {
  const LandPixel({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(0.1),
      child: Container(color: const Color(0xFFA5927D)),
    );
  }
}
